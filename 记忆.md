# Create Mod 项目记忆

## 项目概述
- **项目名称**: Create Mod
- **作者**: 郑zip
- **目标版本**: Mindustry v158.1
- **主要功能**: 基础金属加工系统 + 多节Boss单位移植
- **参考源码**: PU132 (v132) 和 PU_V8
- **GitHub**: https://github.com/ZZW-vs/MindustryJavaMod.git

## 核心架构

### 1. 模组结构
- **主类**: `zzw.TestMod`
- **内容包**: `zzw.content.*`
- **单位系统**: 分为腿部单位、飞行单位、多节虫子单位
- **方块系统**: 金属加工、机械网络、光学系统、热力系统

### 2. 单位分类
| 系列 | 数量 | 状态 | 描述 |
|------|------|------|------|
| Monolith | 14个 | ✅ 已完成 | 机甲系、多足巨构系、辅助无人机系 |
| Scar | 9个 | 🚧 进行中 | 腿部单位(6个) + 飞行单位(3个) |
| 系列三 | 2个 | ⏳ 待开始 | deviation/anomaly |
| End 最终 | 2个 | ⏳ 待开始 | thalassophobia/apocalypse |

### 3. 当前进行中的任务: Scar系列移植

#### Scar系列单位列表
**腿部单位 (6个)**:
- hovos (T1) - 基础腿部单位，磁轨炮
- ryzer (T2) - 中级腿部单位，三磁轨炮
- zena (T3) - 高级腿部单位，三磁轨炮+护盾
- sundown (T4) - 高级腿部单位，多重武器
- rex (T5) - 顶级腿部单位，多重武器+护盾
- excelsus (T6) - 顶级腿部单位，多重武器

**飞行单位 (3个)**:
- whirlwind (T1) - 基础飞行单位，机枪
- jetstream (T2) - 中级飞行单位，导弹
- vortex (T3) - 高级飞行单位，激光

#### 已移植的Scar基础设施
1. **AI控制器**: `DistanceGroundAI.java` - 实现PU132的距离锁定机制
2. **特效系统**: `ScarFx.java` - 完整移植PU132的scar特效
3. **武器系统**: `SaberContinuousLaserBulletType.java` - 连续激光武器
4. **护盾系统**: `DirectionShieldAbility.java` - 方向护盾能力
5. **单位类型**: `UnityUnitType.java` - 增加了abilityRegions和customBackLegs支持
6. **单位注册**: `Z_ScarUnits.java` - 注册9个Scar单位

#### 技术难点
- **API兼容性**: v158.1 vs v132/v155.4的API差异
- **弹药系统**: v158.1移除了单位弹药系统，需要调整逻辑
- **渲染系统**: Draw类方法变化，需要适配新的渲染API
- **单位构造**: constructor字段必须显式指定为`UnitEntity::create`

## 重要技术经验

### 1. API兼容性问题
- **acceleration字段**: v158.1中不存在，已注释掉
- **ammo字段**: v158.1中移除，改为`(!useAmmo || true)`
- **Core.atlas**: 仍然可用，但需要确保贴图路径正确
- **UnitCommand**: v158.1中可能不存在，暂时移除相关检查

### 2. 渲染系统适配
- **Draw类**: v158.1中许多Draw方法被移除或重命名
- **Drawf**: 推荐使用Drawf替代部分Draw方法
- **混合模式**: 某些效果需要显式调用`Draw.blend()`

### 3. 单位系统适配
- **MixedLegUnitEntity**: 改为`mindustry.gen.UnitEntity::create`
- **LegsUnit**: 用于有腿的单位，需要实现Legsc接口
- **防作弊系统**: End阵营单位特有的多槽位无敌帧机制

### 4. 构建系统
- **版本配置**: build.gradle中mindustryVersion已更新为v158.1
- **Jabel编译**: 支持UTF-8编码，中文注释安全
- **构建命令**: `.\gradlew build` 或 `.\gradlew jar`

## 开发流程

### 1. 每个功能的完成流程
1. 代码实现（中文Javadoc注释）
2. 本地构建验证：`.\gradlew build`
3. Git提交：`git add . && git commit -m "中文提交信息"`
4. 推送GitHub：`git push origin master`

### 2. 错误处理
- **编译错误**: 对比v154.3源码和v155.4 sources jar检查API差异
- **运行错误**: 查看游戏日志，定位问题源头
- **贴图错误**: 确保贴图路径正确，使用`Core.atlas.find(name, fallback)`

### 3. 代码规范
- **包名**: 必须为`zzw.content.*`，禁止`src.`前缀
- **注释**: 中文Javadoc，使用`<p>`标签作为空行
- **贴图命名**: 统一使用`create-`前缀
- **bundle key**: 必须带mod前缀（如`unit.create-buffer.name`）

## 重要文件位置

### 核心文件
- `src/zzw/TestMod.java` - 主模组类
- `build.gradle` - 构建配置
- `mod.hjson` - 模组元数据

### 内容文件
- `src/zzw/content/units/` - 单位相关
- `src/zzw/content/blocks/` - 方块相关
- `src/zzw/content/mechanics/` - 机械系统
- `assets/bundles/` - 本地化文件

### 参考文件
- `参考/PU132源代码/` - PU132原版源码
- `参考/PU_V8/` - PU_V8版本源码
- `参考/Mindustry-154.3源码/` - v154.3源码参考

## 当前状态

### 已完成工作
1. ✅ Scar系列基础设施移植（AI、特效、武器、护盾）
2. ✅ 9个Scar单位的基本框架创建
3. ✅ bundle汉化（中英文翻译）
4. ✅ build.gradle版本更新为v158.1
5. ✅ API兼容性修复（acceleration、ammo、UnitCommand等）

### 待完成工作
1. 🚧 编译错误修复（当前有100个编译错误）
2. 🚧 本地构建验证
3. 🚧 Git提交和推送
4. ⏳ Scar系列单位完整功能实现
5. ⏳ 系列三单位移植
6. ⏳ End最终单位移植

### 下一步计划
1. 修复剩余编译错误
2. 完成本地构建验证
3. 提交当前进度到GitHub
4. 继续Scar系列单位的功能完善

## 注意事项

1. **完美还原**: 严格遵循PU132原版机制，禁止简化
2. **中文注释**: 所有代码必须有中文Javadoc注释
3. **构建验证**: 每次修改后必须进行本地构建验证
4. **版本控制**: 及时提交和推送代码变更
5. **用户测试**: 大项完成后需要用户测试验证

---
**最后更新**: 2026-09-13
**当前版本**: 2.2.3
**目标**: 完成Scar系列移植，继续后续系列
## 大地单位子世界交互优化 (2026-09-13, WorldUnitType.java)

### 问题与修复
1. **分类器/物品源无法配置** — 根源: v158 `BlockConfigFragment.updateTableAlign` /
   `BlockInventoryFragment.updateTablePosition` 按建筑自身 x/y 定位 UI, 子世界建筑的
   x/y 是子世界坐标(数值很小), 配置界面飞到地图原点。
   修复: `installSubUiFixer()` 在场景根部加 act 顺序最后的元素, 检测到选中的是子世界
   建筑时用投影后主世界坐标(绕单位旋转)重定位 config/inv 表 (包私有字段走反射缓存)。
2. **无拖拽画线放置** — 旧实现是"按住即时沿光标路径落块"。重写为原版体验:
   `handleSubWorldBuildInput` 拖拽状态机 (dragNone/dragPlace/dragBreak):
   - 放置: 点击开始 → `updateSubLinePlans` 复刻 `iterateLine` (normalizeLine/
     normalizeRectangle + Tile.relativeTo 逐点朝向 + 多方块防重叠) → 松开
     `commitSubPlace` 整条提交; 拖拽中按旋转键 = overrideLineRotation。
   - 拆除: 点击框选 → 红框预览(drawBreakSelection 风格) → 松开 `commitSubBreak` 逐格 breakSub。
   - 未拖拽时单格 ghost 跟随光标 (原版放置观感, mixcol 白/红无效反馈)。
3. **sweepPlans 简化**: 建造模式下原版主世界计划一律清除 (不再锚点转译) — 否则原版
   拖线 flush 的计划会与子世界直接提交叠加, quickRotate 把刚放的方块转错方向。
4. **光标反馈**: 建造模式下放置/悬停可配置子世界建筑 → 手型光标 (补丁 drawTop)。
5. **点击配置补原版 configureSound 音效**。

### 关键经验
- v158 原版参考: InputHandler.iterateLine / drawBreakSelection / tileTapped,
  BlockConfigFragment (v158 才改名, 旧版叫 ConfigFragment)。
- 子世界拖拽必须全程在子世界坐标系计算(拖拽起点记 sub tile), 平台旋转时
  主世界网格的线映射到子世界会变成锯齿斜线。
- Z_Sounds 补了 energyBlast (assets/sounds/energy-blast.ogg 本来就有)。

## 大地单位子世界五项问题修复 (2026-09-13 下午)

1. **电力三问题 (不显示/不能手动连/不自动连)** — 三个独立根因:
   - absorb() 缺 `updateProximity()` 全量重建 (PU132 setup 有, 移植遗漏) → 电力图孤立;
     已补: 先 updateProximity 全部, 再应用 tmpLinks (configureAny 写入重映射链接)。
   - 电力链接重映射只算未写: absorb 里 nseq 存进 tmpLinks 后没写回 power.links →
     激光连线不显示。configureAny 必须在 Vars.world=unitWorld 上下文 (内部 world.build)。
   - placeSub 即时路径漏调 `placed()` (原版 Build.beginPlace 的组成步骤) → 新放节点
     不自动连线; 已补 (在 updateProximity 之后)。
   - 手动连接: 点击流程复刻原版 tileTapped 的 onConfigureBuildTapped 分支 (必须在
     Trigger.update 同帧同步捕获 config 选中建筑, 因为原版 tileTapped(null) 随后 hideConfig);
     新建 SubConfigFragment (zzw/ui) 作为子世界配置宿主 — 原版 OverlayRenderer 会把
     电力节点配置可视化画到地图原点, 子世界版的 drawConfigure 由 drawBody 在投影+子世界
     上下文绘制 (cfgSel.drawConfigure())。
2. **光束连偏** — TurretBuild.targetPos 是主世界坐标, drawBody 投影期望子世界坐标;
   画前逆变换 (p-单位位置).rotate(-(r))+子世界中心, 画完恢复。
3. **护盾力场悬空跟随** — ShieldBulletType (Shielder) 长寿命驻场子弹被 followBullets
   拖着走; 已排除 (原版无子弹跟随, 力场留在原地)。
4. **传送带速度不一** — 同问题1: 缺 updateProximity → ConveyorBuild.nextc/blends 未建,
   物品交接失败卡停。
5. **容器拿不到物品** — 原版 requestItem 服务端校验 player.within(build, itemTransferRange),
   子世界坐标距离恒超限; canPick 客户端同判。新建 SubInventoryFragment (zzw/ui, 整份适配
   BlockInventoryFragment): 子世界建筑直接本地转账 (removeStack+addItem, Call.takeItems
   服务端语义), 特效/定位用投影坐标; 主世界建筑行为不变。
   两 Fragment 由 installSubUi() 挂到 scene.root + 注入 subPositionResolver
   (findSubOwner+projectToOwner, 已改 public static)。

### 注意
- Z_Planets.java ContentType 导入用户已自修 (mindustry.ctype)。
- 上一轮的反射定位修复器 (subUiFixer/fixSubUiPositions/configTableField) 已删除,
  被 SubConfigFragment 自定位取代。

## 子世界拖拽右键语义对齐原版 (2026-09-13 下午补)

原版 DesktopInput: deselect 与 breakBlock 同绑右键, 分支顺序保证
"放置中右键 = 取消选中方块 (deselect 先命中), 未放置时右键 = 框选拆除"。
handleSubWorldBuildInput 同步该顺序: dragBreak 开始条件加 !in.isPlacing();
拖线(dragPlace)中 in.block==null (原版 deselect 已取消选中) → 整条拖拽取消不提交。
FlagellaDecorationType.java 导入修复: Vec2 在 arc.math.geom (非 arc.util), 补 Tmp 导入。
