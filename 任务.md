# PU132 → v158.1 移植任务文档（续作提示词）

> 本文档供后续会话继续工作时使用。项目：把 PU132（v132 版 Progressive Unification）
> 模组完整移植到 Mindustry v158.1。**要求完美还原，尽量用 PU 原版源码，少自己重写。**

***

## 一、给 AI 的启动提示词（复制到新会话即可用）

```
我在做 Mindustry 模组移植项目：把 PU132 模组（v132）完美移植到 v158.1。
项目目录：d:\mc\Mindustry\My mod\Create
请先阅读项目根目录的「任务.md」了解全部待办、已完成内容和关键经验教训，
再阅读「参考」文件夹结构（PU132源代码 / PU_V8 是主要参考源码），
然后继续做文档里「待办任务」中最优先的事项。
规则：
1. 完美还原移植，尽量用 PU 的源代码，不要自己重写导致效果不一致
2. 代码需要中文 Javadoc 注释，空行用 <p> 标签，核心算法逐步解释
3. package 声明为 zzw.content...，禁止 src. 前缀
4. 做完一个功能：本地 gradlew jar 构建验证 → git 提交（中文提交信息）→ push GitHub
5. 遇到拿不准的先问我，不要自作主张简化机制
```

***

## 二、项目关键信息

| 项                 | 值                                                           |
| ----------------- | ----------------------------------------------------------- |
| 项目路径              | `d:\mc\Mindustry\My mod\Create`                             |
| 构建命令              | `.\gradlew.bat jar`（产物 build/libs/CreateDesktop.jar）        |
| 目标版本              | Mindustry **v158.1**（编译基线 v155.4，build.gradle 可不改）          |
| PU 参考源码           | `参考\PU132源代码\main\src\unity\`、`参考\PU_V8\main\src\unity\`    |
| 原版 Mindustry 源码参考 | `参考\Mindustry-154.3源码\core\src\`（查 API 差异用）                 |
| v155.4 源码 jar     | gradle 缓存里有 sources jar，可解包对比 API                           |
| GitHub            | `https://github.com/ZZW-vs/MindustryJavaMod.git`（master 分支） |
| 贴图目录              | `assets/sprites/`，中文贴图统一放 `create/` 子目录                     |
| 音效目录              | `assets/sounds/end/` 结构保持                                   |

***

## 三、当前进度（2026-08-31 更新）

### 单位移植总览

| 系列                     | 总数        | 完成 | 状态       |
| ---------------------- | --------- | -- | -------- |
| Monolith 系列            | 14（排除3废弃） | 10 | ✅ 已完结（能量环4个按用户要求放弃） |
| Scar 系列                | 9         | 0  | 未开始      |
| 系列三（deviation/anomaly） | 2         | 0  | 未开始      |
| End 最终单位               | 2         | 0  | 未开始      |

### Monolith 系列明细（2026-09-04 完结）

**已完结（10 个，在 Z_MonolithUnits.java，load 已接入 TestMod）：**

* `monolithSoul`(巨石灵魂)：MonolithSoulEntity 已按 PU_V8 生命周期移植（生命衰减/实体化/50%碎裂/加入消散+转移特效），
  无 Soul/monolithWorld 系统做了简化（不转移增益、只接受友方容器），TODO 已注明
* 机甲系：`stele`(石碑)、`pedestal`(基座，用 ChargeShotgunWeapon+Ricochet)、`pilaster`(壁柱)
* 多足巨构系：`pylon`、`monument`、`colossus`、`bastion`（用 UnityBullets/Ricochet/LightningSpawnAbility）
* 辅助无人机系：`adsect`、`comitate`（AssistantAI 未移植走默认 AI，代码留 TODO）

**放弃移植（4 个，按用户 2026-09-04 要求）：**

* `stray`、`tendence`、`liminality`、`calenture`：依赖 EnergyRingWeapon / HelixLaserBulletType / UnitDecal，不再移植

**收尾修复记录（2026-09-04）：**

* 贴图引用去 "unity-" 前缀：`monolith-chain`、`line-shade`（Z_MonolithUnits + JoiningBulletType）、
  `hcircle`（TexturedTrail）、`block-N`（SupernovaTurret）
* 10 个单位中英文 bundle 汉化已补齐（bundle_zh_CN.properties / bundle.properties）

### 已移植的基础设施（Monolith 相关）

| 类                                                                 | 位置                                 | 状态 |
| ----------------------------------------------------------------- | ---------------------------------- | -- |
| MultiTrail / Trails / TexturedTrail / FixedTrail                  | `src/zzw/content/units/graphics/`  | ✅  |
| ChargeFx / LineFx / ParticleFx / SpecialFx / TrailFx / MonolithFx | `src/zzw/content/units/effects/`   | ✅  |
| UnityDrawf / UnityPal / UnityUtils                                | 各对应目录                              | ✅  |
| ChargeShotgunWeapon                                               | `src/zzw/content/units/weapons/`   | ✅  |
| RicochetBulletType                                                | `src/zzw/content/units/bullets/`   | ✅  |
| UnityBullets（pylonLaser/pylonLaserSmall/monumentRailBullet 等）     | `src/zzw/content/units/bullets/`   | ✅  |
| JoiningBulletType（stele 聚合弹）                                      | `src/zzw/content/units/bullets/`   | ✅  |
| LightningSpawnAbility（colossus/bastion 雷电球）                       | `src/zzw/content/units/abilities/` | ✅  |
| Engine / MultiEngine / EngineHold                                 | `src/zzw/content/units/types/`     | ✅  |

***

## 四、待办任务（按优先级）

### Monolith 系列收尾 —— ✅ 2026-09-04 已完结

1. ~~**Z\_MonolithUnits.load() 接入主加载流程**~~ —— 已接入 TestMod.loadContent()
2. ~~**monolithSoul + 灵魂机制**~~ —— 已按 PU_V8 生命周期简化移植（无 Soul 系统的完整版，
   如 souls 吸收/maxSouls 增益需后续评估是否补全）
3. ~~**贴图名核对**~~ —— 已修正 unity- 前缀残留
4. ~~**bundle 汉化**~~ —— 10 个单位中英文条目已补齐
5. AssistantAI（adsect/comitate 的助理 AI）仍留 TODO，可选后续任务

### 优化/修复（用户测试后反馈，与单位移植穿插做）

1. **create-desolation 触手**：不智能、每节中间太宽有缝隙 → 与 apocalypse 移植一起解决

### 后续系列（按顺序）

1. **Scar 系列**（9 个）：unity-hovos、unity-ryzer 等
2. **系列三**（2 个）：unity-deviation、unity-anomaly（有动画）
3. **End 最终单位**（2 个，最难）：

   * unity-thalassophobia：尾部动画麻烦

   * unity-apocalypse：触手制作麻烦（与 desolation 触手一起重做）

***

## 五、经验教训（踩过的坑，勿重复）

* **ExpHolder 只有一个**：`zzw.content.exp.ExpHolder`。任何新经验方块都实现它，
  经验球 ExpOrbs.ExpOrb 的 `instanceof` 只认这个

* **子弹消散触发逻辑**：覆写 `hit` 必须覆写 4 参版本
  `hit(Bullet b, float x, float y, boolean createFrags)`，3 参会被 despawned() 绕过

* **arc Seq.toArray()**：返回 Object\[]，不能直接强转元素数组（ClassCastException），
  直接遍历 Seq

* **ObjectMap 迭代器禁止嵌套**：迭代中会 clear 的场景先收集后处理
  （Light.java 的 children 处理已是此模式）

* **对象池双重入池**：任何 queueRemove 要幂等（pendingRemove 标志），
  pool 取对象时校验 removed 状态

* **GraphModule.read()**：读档前必须检查数组 null，否则 NPE 损坏存档

* **v155.4 无 pierceShields 字段**：用 `absorbable = false` 替代

* **BulletType despawnHit**：v155 默认 false，需要消散生效的手动设 true

* **多节单位**：段身 hidden=true、禁 AI、位置头部设置、hitSize 按类型
  （oppression 180f / devourer 52f）、武器按 segmentIndex 分组

* **continuous 激光方向固定**：rotate=false + shootCone=360f（详见 Light/压迫者方案）

* **UnityUnitType 构造**：必须显式 `constructor = UnitEntity::create`（v155.4+ 要求）

* **自定义 Entity**：需 ZEntityRegister 注册 EntityMapping.idMap 取唯一 classId

* **渲染层级**：黑色菱形/激光等特效 `Draw.z(Layer.flyingUnit + 1f)`；
  黑色激光 draw() 开头 `Draw.blend()` 重置 alpha 混合

* **bundle key 要带 mod 前缀**（如 `unit.create-buffer.name`）汉化才生效

* **Jabel 编译**：源码编码 UTF-8 已在 build.gradle 配好，中文注释安全

* **拆网络中的 multi 方块**：BaseGraph.isAtriculationPoint 邻居空时返回 false，
  否则 IndexOutOfBoundsException（已修，防止回归）

* **PU132 单位移植流程**（本次新增）：按"简单→中等→困难"逐个移植、遇到依赖再写、
  每个单位编译验证不积累错误；贴图用户已拷贝只需核对命名

***

## 六、工作流约定（用户要求）

1. 完美还原，**禁止简化替代**；武器机制完整还原
2. 代码中文 Javadoc、`<p>` 空行标签、核心算法逐步注释（面向新手开发者）
3. 每完成一项：本地构建 → git 提交（中文信息）→ 推 GitHub
4. 大项做完让用户测试再继续下一项
5. 拿不准的、名字对不上的，**直接问用户**
6. 难以实现的功能先跳过做简单的，最后和用户讨论（最好全部移植）
7. 遇到 `[E]` 错误日志：先在 `参考\Mindustry-154.3源码` 或 gradle 缓存的
   v155.4 sources jar 里核对 API 差异，再动代码
8. 移植顺序：先简单后困难，逐个移植，遇到依赖再写，不要预先铺太多基础设施

